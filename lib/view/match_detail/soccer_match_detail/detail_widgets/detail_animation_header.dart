import 'package:flutter/material.dart';

class DetailAnimationHeader extends StatelessWidget {
  const DetailAnimationHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}